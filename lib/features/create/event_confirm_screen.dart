import 'package:flutter/material.dart';

class EventConfirmScreen extends StatelessWidget {
  const EventConfirmScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
